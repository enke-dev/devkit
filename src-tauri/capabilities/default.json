{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the DevKit window. The sidecar is spawned from Rust, so no shell permissions are exposed to the frontend; the updater and a restart are, because installing a new version is asked for from the UI.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
