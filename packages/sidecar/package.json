{
  "name": "@devkit/sidecar",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "check": "bun run --parallel check:*",
    "check:src": "tsc -p tsconfig.json --noEmit",
    "check:scripts": "tsc -p scripts/tsconfig.json",
    "verify:screencast": "node scripts/verify-screencast.ts",
    "verify:settle": "node scripts/verify-settle.ts",
    "verify:recovery": "node scripts/verify-recovery.ts",
    "lint": "bun run --parallel lint:*",
    "lint:prettier": "bun --bun prettier --config prettier.config.ts --check .",
    "lint:eslint": "bun --bun eslint --config eslint.config.ts",
    "format": "bun run --sequential format:*",
    "format:prettier": "bun --bun prettier --config prettier.config.ts --write .",
    "format:eslint": "bun --bun eslint --config eslint.config.ts --fix"
  },
  "dependencies": {
    "@devkit/protocol": "workspace:*",
    "playwright": "1.63.0"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "5.9.3"
  }
}
